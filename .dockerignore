**/.git           # Keeps the Git repository metadata out of deployments
**/.gitignore     # Avoids sending your .gitignore file itself if not needed
**/.vs            # Ignores Visual Studio-specific settings
**/.vscode        # Ignores Visual Studio Code-specific settings
**/bin            # Omits compiled binary files, as Render will build from source
**/obj            # Omits intermediate object files, also compiled during build
**/node_modules   # Omits Node.js modules if you’re not using them directly
**/docker-compose* # Omits Docker Compose files, typically not used on Render
