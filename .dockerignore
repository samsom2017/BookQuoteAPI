# Ignore IDE/Editor files
**/.classpath
**/.project
**/.settings
**/.vs
**/.vscode
**/*.*proj.user

# Ignore local environment files and Docker-related files (unless you need Docker for deployment)
.env
docker-compose
Dockerfile  # Uncomment this line if Render is using Dockerfile; otherwise, remove it to include Dockerfile
.dockerignore

# Ignore build artifacts
**/bin
**/obj
**/node_modules
**/*.dbmdl
**/*.jfm
**/npm-debug.log

# Ignore Kubernetes/helm and Render YAML configuration files (if not needed)
**/azds.yaml
**/charts
**/secrets.dev.yaml  # Secrets should be managed securely via Render's environment variables, not included in code
**/values.dev.yaml

# Git files
.git
.gitignore
!**/.gitignore  # Ensure the main .gitignore file itself is included
!.git/HEAD
!.git/config
!.git/packed-refs
!.git/refs/heads/

# Licenses and documentation
LICENSE
README.md
ads/**